/* Teste de Comentario Multlines */

// Teste de Comentario simples
var and = true && !teste || false;

let numInt = 1000000000 - 55.99;

numInt++;
numInt -= 10;

if(numFloat >= numInt) {
    console.Write("Teste 'string'");
} if(numFloat == numInt) {
    console.Write('Teste "string"');
} esle { // Erro proposital
    console.Write("Barra invertida \\ \" \'");